import 'package:flutter/material.dart';

class AppPaddings {
  static const EdgeInsets bottomAreaPadding = EdgeInsets.all(10);

  static const EdgeInsets pagePadding = EdgeInsets.all(10);
  static const EdgeInsets cardPadding = EdgeInsets.all(10);

  static const EdgeInsets sectionContentPadding =
      EdgeInsets.symmetric(horizontal: 5);

  static const EdgeInsets sectionTitlePadding =
      EdgeInsets.symmetric(horizontal: 10);
}
